quarkus.kubernetes.ingress.expose=true
quarkus.kubernetes.ingress.host=chapter-6-quarkus-rest-cloud-config.127.0.0.1.nip.io

quarkus.kubernetes.ports.http.host-port=8080
quarkus.kubernetes.ports.http.container-port=8080

# use the same name as the application name that was configured when standing up the Config Server
quarkus.application.name=greeting-client

# enable retrieval of configuration from the Config Server - this is off by default
quarkus.spring-cloud-config.enabled=true

# configure the URL where the Config Server listens to HTTP requests
#quarkus.spring-cloud-config.url=http://spring-cloud-config-server.config-storage:80
quarkus.spring-cloud-config.url=http://localhost:8888
#quarkus.kubernetes.env.vars.QUARKUS_SPRING-CLOUD-CONFIG_URL=http://spring-cloud-config-server.config-storage

quarkus.container-image.registry=quay.io
quarkus.container-image.group=cmoulliard
quarkus.container-image.name=chapter-6-quarkus-rest-cloud-config
quarkus.container-image.tag=1.0
quarkus.container-image.build=true
quarkus.container-image.push=true
#quarkus.container-image.username=<USERNAME>
#quarkus.container-image.password=<PASSWORD>
quarkus.container-image.username=cmoulliard+dabou
quarkus.container-image.password=DHEBYPLOA7LQFAYAX00C11I3LB13ZPJ75UVXP37OND16WD2LKP43ZHL8W2JMQB8N


